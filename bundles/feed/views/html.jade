!!!5
html
  head
    style
      li { padding: 4px 0; }
  body
    ul
      for tweet in tweets
        li [#{relativeDate(tweet.created)}] #{tweet.user.screenName}: !{urlize(tweet.text, true)}
          a(href="http://twitter.com/#{ tweet.user.screenName }/status/#{ tweet.tweetId }") (tweet)

