!!!5
html
  head
    style
      li { padding: 4px 0; }
  body
    ul
    - tweets.forEach(function(tweet) {
      li [#{dateFormat(tweet.created, "-7")}] #{tweet.user_screen_name}: !{urlize(tweet.text)}
    - });

